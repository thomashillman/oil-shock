-- Snapshot columns are introduced in 0002_live_contract.sql.
-- Keep this migration as a no-op placeholder so the migration index remains
-- stable without re-adding duplicate columns on fresh databases.
SELECT 1;
